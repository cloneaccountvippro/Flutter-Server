class Test {
    constructor(userId, category, result, timeCompleted, topicId) {
        this.userId = userId,
        this.category = category,
        this.result = result,
        this.timeCompleted = timeCompleted,
        this.topicId = topicId
    }
}
