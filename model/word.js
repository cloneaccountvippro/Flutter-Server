class Word{
    constructor(word, vocab, meaning, topicId) {
        this.word = word;
        this.vocab = vocab;
        this.meaning = meaning;
        this.topicId = topicId
    }
}

export default Word;
